// Struct para poder almacenar los puntos
typedef struct Point {
    float x;
    float y;
    float z;
    float w;
} Point;

//Impresion de los puntos
void printPoints(Point * points, int pointIdx);